/******************************************************************************
    SymbolTableApplications_Sets.c
	==============================

	Sets : Collection of distinct keys
	
	Primitive APIs::
	==============
	add(key) : add the key to set
	contains(key) : is the key in set?
	remove(key) : remove the key from the set
	size()	: return the number of keys in the set
	iterator() : iterate through keys in the set
	
	Real World Application : 
	1) Exception Filter - to filter the words (sets) either as whitelist or 
							blacklist
	2) Dictionary Lookup - e.g. csv file example (to interchange key-value pair 
							and give queries)
	3) Indexing Clients - e.g. to index all files on computer to find out 
							the file which contains the query string.
	4) 
							
							
							
*******************************************************************************/

#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
#include <string.h>

typedef int bool;
#define TRUE 1
#define FALSE 0



int main()
{
	
	
	getch();
    return 0;
}
